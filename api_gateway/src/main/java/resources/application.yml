spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes: # Define routes
        - id: user-service # user service
          uri: http://localhost:8081 # temp url for now, define url
          predicates:
            - Path=/api/users/** # any routes that have /api/users/ go to user microservice and serviced on that end
